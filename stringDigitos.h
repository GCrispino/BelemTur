#include <iostream>

using namespace std;

//Função auxiliar utilizada para verificar se uma string dada é formada por números, para validar alguns atributos
bool stringDigitos(const string &);
string maiuscula(const string &);